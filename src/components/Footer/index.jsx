import React from "react";
import PropTypes from "prop-types";

Footer.propTypes = {};

function Footer(props) {
  return (
    <div style={{ fontSize: "3rem", textAlign: "center", color: "teal" }}>
      This is the footer ':)'.!
    </div>
  );
}

export default Footer;
